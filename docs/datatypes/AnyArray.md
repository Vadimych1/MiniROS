# AnyArray

This datatype encodes serializable Python types (`str`, `int`, `bool`, etc.) and also `dicts` or `lists` of them

Note that this **does NOT work with MiniROS datatypes like `Vector` or `NamedComposedDatatype` for now**